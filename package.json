{
  "name": "crypto-hub-bot",
  "version": "0.1.0",
  "description": "Crypto Hub Bot — MVP scaffold (Express, MongoDB, node-cron, Gemini placeholder)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --watch src --exec node src/index.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "pm2:start": "pm2 start ecosystem.config.cjs",
    "pm2:stop": "pm2 stop crypto-hub-bot",
    "pm2:restart": "pm2 restart crypto-hub-bot",
    "pm2:delete": "pm2 delete crypto-hub-bot",
    "pm2:logs": "pm2 logs crypto-hub-bot",
    "pm2:monit": "pm2 monit",
    "pm2:status": "pm2 status"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^1.30.0",
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.4.0",
    "cron": "^2.4.4",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.3.1",
    "node-cron": "^3.0.2",
    "telegraf": "^4.12.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
